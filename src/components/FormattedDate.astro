---
interface Props {
	date: Date;
	lang: string;
}

const { date, lang } = Astro.props;
console.log(date, lang);
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString(lang === 'en' ? "en-us" : "pt-br", {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
